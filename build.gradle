task cleanAll(type: GradleBuild) {
    tasks = ['cli:clean', 'gui:clean']
}

task linkAll(type: GradleBuild) {
    tasks = ['cli:link', 'gui:link']
}

task wrapper(type: Wrapper) {
    gradleVersion = '4.2.1'
}

